<UserControl
    x:Class="Dispatch.View.Fragments.ConnectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Dispatch.View.Fragments"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="FTPConnectView">
            <local:FTPConnectView OnConnected="ConnectView_OnConnected" />
        </DataTemplate>
        <DataTemplate x:Key="SFTPConnectView">
            <local:SFTPConnectView OnConnected="ConnectView_OnConnected" />
        </DataTemplate>
        <local:ConnectViewDataTemplateSelector
            x:Key="ConnectViewDataTemplateSelector"
            FTPDataTemplate="{StaticResource FTPConnectView}"
            SFTPDataTemplate="{StaticResource SFTPConnectView}" />
    </UserControl.Resources>

    <ScrollViewer
        Background="#191919"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <StackPanel
            MaxWidth="380"
            Margin="64"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <ComboBox Name="ComboBox" SelectedIndex="0">
                <ComboBoxItem Content="FTP" />
                <ComboBoxItem Content="SFTP" />
            </ComboBox>
            <ContentPresenter Content="{Binding SelectedIndex, ElementName=ComboBox}" ContentTemplateSelector="{StaticResource ConnectViewDataTemplateSelector}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
