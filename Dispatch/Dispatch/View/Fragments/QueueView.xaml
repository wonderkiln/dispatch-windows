<UserControl
    x:Class="Dispatch.View.Fragments.QueueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Dispatch.View.Fragments"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Label
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Content="Queue View" />
    </Grid>

    <!--<ListBox
        Padding="0"
        Background="Transparent"
        BorderThickness="0"
        ItemsSource="{Binding ViewModel.Items, RelativeSource={RelativeSource AncestorType={x:Type local:QueueView}}}"
        ScrollViewer.HorizontalScrollBarVisibility="Disabled">
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border>
                                <ContentPresenter Margin="{TemplateBinding Padding}" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Grid Margin="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock TextWrapping="Wrap">
                            <Run FontWeight="SemiBold" Text="{Binding Source.Path, Mode=OneWay}" />
                            <Run
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="9"
                                Text="&#xEBE7;" />
                            <Run FontWeight="SemiBold" Text="{Binding Destination.Path, Mode=OneWay}" />
                        </TextBlock>
                        <ProgressBar
                            Height="14"
                            Margin="0,4,0,0"
                            Maximum="100"
                            Minimum="0"
                            Value="{Binding Progress.TotalProgress, Mode=OneWay}" />
                    </StackPanel>
                    <Button
                        Grid.Column="1"
                        Margin="8,0,0,0"
                        VerticalAlignment="Center"
                        Command="{Binding CancelCommand, Mode=OneWay}"
                        Content="&#xE711;" />
                </Grid>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>-->
</UserControl>
