<Window
    x:Class="Dispatch.View.Windows.AboutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:a="clr-namespace:Dispatch"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:c="clr-namespace:Dispatch.Converters"
    xmlns:h="clr-namespace:Dispatch.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="About Dispatch"
    Width="300"
    Height="400"
    AllowsTransparency="False"
    Background="{DynamicResource PrimaryBackgroundSolidColorBrush}"
    ResizeMode="NoResize"
    Style="{StaticResource ChromelessWindowStyle}"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Window.Resources>
        <c:DateToRelativeStringConverter x:Key="DateToRelativeStringConverter" />
        <x:Static x:Key="License" Member="a:App.License" />
    </Window.Resources>

    <StackPanel
        Margin="-12,24,24,24"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="54" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Image
                Width="36"
                Height="36"
                VerticalAlignment="Center"
                Source="{x:Static h:Icons.Icon}" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <Label Content="{x:Static h:Constants.APP_NAME}" FontSize="16" />
                <Label Content="{x:Static h:Constants.VERSION_STRING}" Opacity="0.5" />
                <Label Content="{Binding Device.Status, Source={StaticResource License}}" Opacity="0.5" />
                <Label Content="{Binding Device.Device.ExpiresAt, Converter={StaticResource DateToRelativeStringConverter}, Source={StaticResource License}}" Opacity="0.5" />
                <Button Command="{Binding InstallTrialCommand, Source={StaticResource License}}">Trial</Button>
                <TextBox Text="{Binding LicenseKey, Source={StaticResource License}}" />
                <Button Command="{Binding InstallLicenseCommand, Source={StaticResource License}}">License</Button>
                <Button Command="{Binding RemoveLicenseCommand, Source={StaticResource License}}">Remove License</Button>
            </StackPanel>
        </Grid>
    </StackPanel>
</Window>
